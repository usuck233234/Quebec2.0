import java.util.Collections;
import java.util.HashSet;
import java.util.Set;

void keyPressed(){
  if(key == ' ') println("HI!");
  if(key == 'f') println("press F to pay respects");
  if(key == 'f') init();
}
